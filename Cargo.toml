[package]
name = "umbral-patcher"
version = "0.1.0"
edition = "2024"

[dependencies]
byteorder = "1.5"
clap = { version = "4.6", features = ["derive"] }
crc = "3.4"
smallvec = { version = "1.15", features = ["const_generics", "union"] }
